import { ZigBeeDevice } from 'homey-zigbeedriver';
import { ZCLNode } from 'zigbee-clusters';
import { DefaultConfiguration, ReportingConfiguration } from '../lib/attributeDevice';
type ArgumentOverrides = DefaultConfiguration & ReportingConfiguration & {
    onOffCapabilityId: string | false;
    maxDimValue: number;
};
export default function initDimDevice(device: ZigBeeDevice, zclNode: ZCLNode, { capabilityId, onOffCapabilityId, maxDimValue, minInterval, maxInterval, minChange, endpointId, }?: Partial<ArgumentOverrides>): Promise<void>;
export {};
