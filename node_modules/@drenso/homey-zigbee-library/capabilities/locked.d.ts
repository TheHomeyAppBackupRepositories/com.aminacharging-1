import { ZigBeeDevice } from 'homey-zigbeedriver';
import { ZCLNode } from 'zigbee-clusters';
import { LockState } from "../lib/clusters/DoorLockCluster";
import { DefaultConfiguration } from '../lib/attributeDevice';
type ArgumentOverrides = DefaultConfiguration & {
    reportParser: (value: LockState) => unknown;
};
export default function initLockedDevice(device: ZigBeeDevice, zclNode: ZCLNode, { capabilityId, reportParser, endpointId, }?: Partial<ArgumentOverrides>): Promise<void>;
export {};
