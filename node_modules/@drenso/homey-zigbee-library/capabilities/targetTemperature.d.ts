import { AttributeConfiguration } from '../lib/attributeDevice';
import { ZigBeeDevice } from 'homey-zigbeedriver';
import { ZCLNode } from 'zigbee-clusters';
type ArgumentOverrides = AttributeConfiguration;
export default function initTargetTemperatureDevice(device: ZigBeeDevice, zclNode: ZCLNode, { capabilityId, cluster, attributeName, minChange, minInterval, maxInterval, endpointId, }?: Partial<ArgumentOverrides>): Promise<void>;
export {};
