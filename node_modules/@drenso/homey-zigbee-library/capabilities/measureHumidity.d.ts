import { ZigBeeDevice } from 'homey-zigbeedriver';
import { ZCLNode } from 'zigbee-clusters';
import { AttributeConfiguration } from '../lib/attributeDevice';
type ArgumentOverrides = AttributeConfiguration;
export default function initMeasureHumidityDevice(device: ZigBeeDevice, zclNode: ZCLNode, { capabilityId, cluster, attributeName, minChange, minInterval, maxInterval, endpointId, }?: Partial<ArgumentOverrides>): Promise<void>;
export {};
