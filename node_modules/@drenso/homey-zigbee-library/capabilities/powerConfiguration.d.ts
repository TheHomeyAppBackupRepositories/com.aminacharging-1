import { ZigBeeDevice } from 'homey-zigbeedriver';
import { ZCLNode } from 'zigbee-clusters';
import { DefaultConfiguration, ReportingConfiguration } from '../lib/attributeDevice';
type ArgumentOverrides = DefaultConfiguration & ReportingConfiguration;
export default function initPowerConfigurationDevice(device: ZigBeeDevice, zclNode: ZCLNode, { capabilityId, minChange, minInterval, maxInterval, endpointId, }?: Partial<ArgumentOverrides>): Promise<void>;
export {};
