"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.removeCapabilityIfExists = exports.addCapabilityIfNotExists = void 0;
async function addCapabilityIfNotExists(device, capabilityId) {
    if (device.hasCapability(capabilityId)) {
        return;
    }
    await device.addCapability(capabilityId);
}
exports.addCapabilityIfNotExists = addCapabilityIfNotExists;
async function removeCapabilityIfExists(device, capabilityId) {
    if (!device.hasCapability(capabilityId)) {
        return;
    }
    await device.removeCapability(capabilityId);
}
exports.removeCapabilityIfExists = removeCapabilityIfExists;
